# B组 用例图（图4–图11）

> 数据来源：`docs/design/00_总体架构与技术设计.md`（§5 角色与权限矩阵、§6 界面清单、§7 核心闭环时序、§8 模块依赖）、`docs/design/01~07_*_详细设计.md` 各文档 §2 功能需求清单（FR编号）与 §7 界面设计、`docs/design/08_集成与一致性报告.md` §四 全局FR索引总表（117条）、`docs/design/09_设计修订说明.md`（v3.1 reconcile 范围裁决：FR-M5-21~24 内推本期标 Could 不落代码，图中仍按设计文档保留以还原完整需求分析）。
>
> 用例图 Mermaid 不擅长表达（参与者-用例的关联/include/extend 用 Mermaid 画会很别扭），本组全部 8 张图统一提供 **drawio / Visio 的"元素框 + 连线"清单**，不提供 mermaid 代码块（本文件 mermaid 代码块数 = 0，符合用例图应有的表达方式）。
>
> 角色代码统一沿用地基 §5：`GUEST`(访客/未登录)、`STUDENT`(在校生)、`ALUMNI`(毕业生)、`ADMIN`(管理员)。系统自身作为触发方（定时任务/事件监听）在部分图中作为独立的"系统"参与者画出，是本项目状态机自动流转与事件解耦风格的真实体现，不是臆造。

---

## 图4 全局用例图

- **图类型**：用例图
- **放报告**：第四章 §3.2（需求分析总览）
- **要画什么（元素清单）**：
  - **参与者（4个）**：`GUEST`（访客/未登录）、`STUDENT`（在校生）、`ALUMNI`（毕业生）、`ADMIN`（管理员）。
  - **系统边界**：一个大矩形，标题"成长导航平台"，内部按 M1–M7 七个模块分区（用虚线框分组，框标题为模块名，对应地基 §8 模块划分），每个分组内放该模块最具代表性的用例，作为"总览"而非穷举117条FR。
  - **M1 用户与认证分组**（对应 FR-M1-01/02/08/09/10/14/15）：
    - 用户注册、登录、提交在校生认证申请、提交毕业生认证申请(邀请码)、提交毕业生认证申请(人工+担保)、批量生成毕业生邀请码、认证终审
  - **M2 成长画像与校友路径分组**（对应 FR-M2-01/03/06/09）：
    - 维护在校生画像、新建/编辑校友路径卡、浏览校友路径卡列表、路径推荐
  - **M3 经验知识库分组**（对应 FR-M3-01/09/10/12）：
    - 创建知识条目、知识条目列表(分类筛选)、全文搜索、提交三态评价
  - **M4 结构化求助分组★系统灵魂**（对应 FR-M4-01/02/06/10/11）：
    - 发布求助单、求助-校友路由匹配(系统自动)、提交模板化回答、采纳最佳回答、采纳后生成知识候选
  - **M5 机会与组队分组**（对应 FR-M5-01/06/11/13）：
    - 发布机会、机会列表(类型筛选/即将截止)、发起队伍、申请加入队伍
  - **M6 成长时间线分组**（对应 FR-M6-01/05/07/08）：
    - 维护时间线模板、查看我的成长时间线、选择/切换发展路线、标记节点个人进度
  - **M7 平台管理与内容治理分组**（对应 FR-M7-01/09/11/13/20）：
    - 统一审核队列、提交举报、处理举报、标签维护、运营数据统计看板
  - **系统**参与者（放在边界右侧或下方，代表定时任务/事件监听）：连接"求助-校友路由匹配""知识候选自动完整性/隐私预检"两个系统自动用例。
- **怎么画（结构描述）**：
  1. 画一个横向大矩形作系统边界，标题写在顶部居中"成长导航平台"。
  2. 边界内部从左到右/从上到下用 7 个虚线圆角框划分 M1–M7 七个分组区域（可参照地基 §8 依赖图的模块排列顺序：M1→M2/M4→M3/M5→M6→M7），每个分组框内放 3–6 个用例椭圆（如上文清单）。
  3. 参与者放在系统边界**左侧**：从上到下依次 `GUEST`、`STUDENT`、`ALUMNI`；`ADMIN` 放在**右侧**单独一列（体现"治理端"与"用户端"分离）；"系统"参与者放在**下方**。
  4. 关联线画法：`GUEST` 连"用户注册""登录""知识条目列表(分类筛选)""全文搜索""机会列表"（认证前只读分层，见 M1 §6.4）；`STUDENT` 连 M1/M2/M3/M4/M5/M6 分组内的对应用例；`ALUMNI` 连"提交毕业生认证申请""新建/编辑校友路径卡""发布机会"等；`ADMIN` 只连 M7 分组与"认证终审""批量生成毕业生邀请码"。
  5. 核心闭环（地基 §7"系统灵魂"）用一条加粗虚线箭头链把 M4 分组的"发布求助单"→"求助-校友路由匹配"→"提交模板化回答"→"采纳最佳回答"→"采纳后生成知识候选"→ M3 分组"创建知识条目"（候选态）→ M7 分组"统一审核队列"→ M3"知识条目列表"依次串起来，并在旁批注"核心闭环，重点演示"，呼应地基文档原话。
  6. include 关系：`发布求助单` --<<include>>--> `求助-校友路由匹配`；`采纳最佳回答` --<<include>>--> `采纳后生成知识候选`；`提交在校生认证申请` --<<include>>--> `认证终审`前置的自动核验（可简化为箭头注"自动核验"）。
  7. extend 关系：`认证终审` --<<extend>>--> 一个虚线延伸小注"打回补充材料"（RETURNED分支，非必然发生）。
- **可渲染源码或画法**：用例图 Mermaid 不擅长，按上文"怎么画"的 drawio/Visio 元素框 + 连线清单摆放即可（7个分组虚线框 + 4主体参与者 + 1系统参与者 + 约28个用例椭圆 + 1条加粗核心闭环链）。
- **工具建议**：drawio（推荐，分组框用"容器"功能天然支持）；备选 Visio。

---

## 图5 M1 用户与认证 模块用例图

- **图类型**：用例图
- **放报告**：第四章 §3.3
- **要画什么（元素清单）**：
  - **参与者**：`GUEST`、`STUDENT`、`ALUMNI`、`ADMIN`；额外一个**系统**参与者（代表"模拟统一身份认证核验"自动核验与"担保超时"定时任务）。
  - **用例椭圆（对应 FR-M1-01~17，共 17 条，逐条列出）**：
    1. 用户注册（GUEST，FR-M1-01）
    2. 登录（GUEST，FR-M1-02）
    3. Token刷新（STUDENT/ALUMNI/ADMIN，FR-M1-03）
    4. 登出（已登录，FR-M1-04）
    5. 查看当前用户信息（已登录，FR-M1-05）
    6. 修改基本信息（已登录，FR-M1-06）
    7. 修改隐私设置（已登录，FR-M1-07）
    8. 提交在校生认证申请（STUDENT且auth_status≠VERIFIED，FR-M1-08）
    9. 提交毕业生认证申请(邀请码)（ALUMNI且auth_status≠VERIFIED，FR-M1-09）
    10. 提交毕业生认证申请(人工+担保)（ALUMNI且auth_status≠VERIFIED，FR-M1-10）
    11. 担保人确认/拒绝（STUDENT/ALUMNI且auth_status=VERIFIED，FR-M1-11）
    12. 查看认证进度（申请人，FR-M1-12）
    13. 撤回/重新提交认证申请（申请人，FR-M1-13）
    14. 批量生成毕业生邀请码（ADMIN，FR-M1-14）
    15. 认证终审(通过/拒绝/退回)（ADMIN，FR-M1-15，跨M7路由）
    16. 认证前只读权限校验（GUEST/已注册未认证，FR-M1-16，系统性规则，画成被其余写用例 include 的公共用例）
    17. 禁用/启用账号（ADMIN，FR-M1-17）
  - **系统自动用例**（供"系统"参与者连接）：模拟统一身份认证核验（`mockSsoVerify`，对应 §6.1）、担保超时提醒定时任务（对应 §6.3）。
- **怎么画（结构描述）**：
  1. 系统边界矩形标题"M1 用户与认证"。
  2. 左侧从上到下放 `GUEST` → `STUDENT` → `ALUMNI`；右侧放 `ADMIN`；边界下方放"系统"。
  3. 边界内部按业务分两个纵列：左列"账号与会话"（用例1–7），右列"分级身份认证"（用例8–15、17）；用例16单独放在边界顶部中央，因为它是横跨全部写用例的公共规则。
  4. 关联线：`GUEST` 连 1、2；`STUDENT`/`ALUMNI`/`ADMIN` 都连 3–7（画一条从三者向"Token刷新"等的共同关联，或分别画三条平行线）；`STUDENT` 单独连 8；`ALUMNI` 单独连 9、10；`STUDENT`+`ALUMNI`（各自认证通过后）连 11；申请人（即 8/9/10 的发起者）连 12、13；`ADMIN` 连 14、15、17。
  5. include 关系：
     - 用例8 `提交在校生认证申请` --<<include>>--> "模拟统一身份认证核验"（系统用例）
     - 用例10 `提交毕业生认证申请(人工+担保)` --<<include>>--> "查询可担保人候选(同专业已认证)"（可作为用例10内部小分支标注，不必单独画椭圆，用文字批注亦可）
     - 用例1–15、17（除只读浏览外的全部写操作）均 --<<include>>--> 用例16 `认证前只读权限校验`（画一条从各写用例指向16的虚线箭头汇聚，箭头方向由写用例指向16，标注 `<<include>>`）
  6. extend 关系：
     - 用例15 `认证终审` --<<extend>>--> 延伸注"打回补充材料"分支（对应状态机 UNDER_REVIEW→RETURNED）
     - 用例11 `担保人确认/拒绝` --<<extend>>--> 延伸注"两人均CONFIRMED后自动转人工终审"（触发用例15前置状态迁移，非直接调用，用虚线连接并批注）
  7. "系统"参与者用箭头连"模拟统一身份认证核验"与"担保超时提醒定时任务"两个系统用例。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 4+1 参与者、17+2 用例椭圆、约 20 条关联/include/extend 连线。
- **工具建议**：drawio；备选 Visio。

---

## 图6 M2 成长画像与校友路径 模块用例图

- **图类型**：用例图
- **放报告**：第四章 §3.4
- **要画什么（元素清单）**：
  - **参与者**：`STUDENT`、`ALUMNI`、`ADMIN`；一个"全角色（须登录）"泛用参与者位（用于浏览类用例，可用 `STUDENT`/`ALUMNI`/`ADMIN` 三者共同连接表达，不新增参与者符号）。
  - **用例椭圆（对应 FR-M2-01~11，共 11 条）**：
    1. 维护在校生画像（STUDENT，FR-M2-01）
    2. 维护成长标签（STUDENT/ALUMNI，FR-M2-02）
    3. 新建/编辑校友路径卡（ALUMNI，FR-M2-03）
    4. 发布/撤回路径卡（ALUMNI，FR-M2-04）
    5. 配置字段级可见性（ALUMNI，FR-M2-05）
    6. 浏览校友路径卡列表（STUDENT/ALUMNI/ADMIN须登录，FR-M2-06）
    7. 查看路径卡详情（STUDENT/ALUMNI/ADMIN须登录，FR-M2-07）
    8. 按专业聚合去向统计（STUDENT/ALUMNI/ADMIN须登录，FR-M2-08）
    9. 路径推荐（STUDENT，FR-M2-09）
    10. 展示贡献者标识与计数（全角色须登录，FR-M2-10）
    11. 路径卡举报下架/复核恢复（ADMIN，供M7调用，FR-M2-11）
- **怎么画（结构描述）**：
  1. 系统边界标题"M2 成长画像与校友路径"。
  2. `STUDENT` 放左上，`ALUMNI` 放左下，`ADMIN` 放右侧。
  3. 边界内分两个区域：上区"画像与标签"（用例1、2、9、10），下区"校友路径卡"（用例3、4、5、6、7、8、11）。
  4. 关联线：`STUDENT` 连 1、2、9、6、7、8、10；`ALUMNI` 连 2、3、4、5、6、7、8、10；`ADMIN` 连 11、6、7、8（管理员也可浏览/查看，用于举报复核前的核对）。
  5. include 关系：
     - 用例3 `新建/编辑校友路径卡` --<<include>>--> 延伸小注"去向类型分支必填校验"（§6.1，POSTGRAD/EMPLOY等分支）
     - 用例7 `查看路径卡详情` --<<include>>--> 延伸小注"字段级可见性计算"（§6.2，按 SELF/SAME_MAJOR/PUBLIC 脱敏）
     - 用例8 `按专业聚合去向统计` --<<include>>--> 延伸小注"样本门槛(≥20)与二级维度k-匿名(≥5)校验"（§6.3）
     - 用例9 `路径推荐` --<<include>>--> 延伸小注"候选池构建+打分排序+多样性去重"（§6.4规则引擎）
  6. extend 关系：
     - 用例4 `发布/撤回路径卡` --<<extend>>--> 状态机分支"DRAFT↔PUBLISHED"（§4状态图）
     - 用例11 `路径卡举报下架/复核恢复` 用虚线连一个跨模块批注框"由 M7 举报处理(dispatchReportAction)触发调用"，表明该用例是被 M7 用例`处理举报`通过 Service 调用触发，而非本模块用户直接发起（体现"举报受理在M7、下架能力在M2"的分工）。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 3 参与者、11 用例椭圆、约 15 条连线（含4条include、2条extend/跨模块批注）。
- **工具建议**：drawio；备选 Visio。

---

## 图7 M3 经验知识库 模块用例图

- **图类型**：用例图
- **放报告**：第四章 §3.5
- **要画什么（元素清单）**：
  - **参与者**：`GUEST`、`STUDENT`、`ALUMNI`、`ADMIN`；一个**系统**参与者（代表 M4 采纳触发、以及三个定时任务）。
  - **用例椭圆（对应 FR-M3-01~18，共 18 条）**：
    1. 创建知识条目(原创)（STUDENT/ALUMNI/ADMIN已认证，FR-M3-01）
    2. 采纳生成知识候选（系统，由M4触发，FR-M3-02）
    3. 编辑知识条目（作者/认领人/ADMIN，FR-M3-03）
    4. 提交审核（作者/认领人，FR-M3-04）
    5. 知识候选终审通过（ADMIN，FR-M3-05，跨M7）
    6. 知识候选终审退回（ADMIN，FR-M3-06，跨M7）
    7. 手动下线（作者/认领人/ADMIN，FR-M3-07）
    8. 内容认领更新（STUDENT/ALUMNI已认证，FR-M3-08）
    9. 知识条目列表(分类筛选)（全角色含GUEST，FR-M3-09）
    10. 全文搜索（全角色含GUEST，FR-M3-10）
    11. 查看知识条目详情（全角色含GUEST，FR-M3-11）
    12. 提交/更新三态评价（STUDENT/ALUMNI/ADMIN已认证，FR-M3-12）
    13. 查看三态评价统计（全角色含GUEST，FR-M3-13）
    14. 我的知识贡献列表（作者/认领人，FR-M3-14）
    15. 软删除知识条目（作者限非PUBLISHED/ADMIN，FR-M3-15）
    16. 定时任务:过期自动降权（系统，FR-M3-16）
    17. 定时任务:三态反馈驱动降权与预警（系统，FR-M3-17）
    18. 定时任务:长期未维护释放认领（系统，FR-M3-18）
- **怎么画（结构描述）**：
  1. 系统边界标题"M3 经验知识库"。
  2. `GUEST` 放最左（只连读类用例）；`STUDENT`、`ALUMNI` 放左侧中段；`ADMIN` 放右侧；"系统"放下方。
  3. 边界内分三区：左区"创建与编辑"（用例1、3、4、7、8、15），中区"检索与浏览"（用例9、10、11、13、14），右区"审核"（用例5、6，靠近ADMIN）；下方一条"定时任务"横条放用例2、16、17、18。
  4. 关联线：`GUEST` 只连 9、10、11、13（只读，对应"认证前只读分层"）；`STUDENT`/`ALUMNI`/`ADMIN`(已认证)连 1、3、4、7、8、12、14、15、9、10、11、13；`ADMIN` 单独连 5、6。
  5. include 关系：
     - 用例1 `创建知识条目(原创)` --<<include>>--> 延伸小注"公共信息导航强制外链校验(高时效信息不自存红线，§6.1)"
     - 用例10 `全文搜索` --<<include>>--> 延伸小注"MySQL FULLTEXT(ngram)匹配，短关键词兜底LIKE(§6.6)"
     - 用例12 `提交/更新三态评价` --<<include>>--> 延伸小注"upsert+计数原子更新(§6.5)"
     - 用例2 `采纳生成知识候选` --<<include>>--> 延伸小注"自动调用submitForReview进入REVIEWING(§6.3)"
  6. extend 关系：
     - 用例3 `编辑知识条目` --<<extend>>--> "对PUBLISHED内容发起修订"分支（视为轻量重审，直接转REVIEWING，§6.2）
  7. 跨模块批注：用例2 `采纳生成知识候选` 旁画一个虚线框批注"由 M4 `HelpAnswerService.adopt()` 同进程调用`KnowledgeEntryService.createFromHelpAdoption`触发（AFTER_COMMIT异步，见09设计修订说明R3），非本模块用户直接发起"；"系统"参与者连 2、16、17、18 四个自动用例。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 4+1 参与者、18 用例椭圆、约 22 条连线。
- **工具建议**：drawio；备选 Visio。

---

## 图8 M4 结构化求助 模块用例图（系统灵魂/核心闭环枢纽）

- **图类型**：用例图
- **放报告**：第四章 §3.6
- **要画什么（元素清单）**：
  - **参与者**：`STUDENT`（主）、`ALUMNI`、`ADMIN`；一个**系统**参与者（代表路由匹配算法与两个定时任务）。
  - **用例椭圆（对应 FR-M4-01~13，共 13 条）**：
    1. 发布求助单（STUDENT主/ALUMNI/ADMIN已认证，FR-M4-01）
    2. 求助-校友路由匹配(系统自动)（系统，FR-M4-02，★验收核心）
    3. 路由重试与兜底升级(定时任务)（系统，FR-M4-03）
    4. 浏览求助单列表(本专业高频)（登录用户，FR-M4-04）
    5. 查看求助单详情（登录用户，FR-M4-05）
    6. 提交模板化回答（STUDENT/ALUMNI/ADMIN已认证，FR-M4-06）
    7. 编辑本人回答（回答人，FR-M4-07）
    8. 提交追问(限次)（求助人，FR-M4-08）
    9. 回复追问（回答人，FR-M4-09）
    10. 采纳最佳回答（求助人，FR-M4-10）
    11. 采纳后生成知识候选(调用M3)（系统，FR-M4-11）
    12. 关闭求助单（求助人/系统，FR-M4-12）
    13. 撤回求助单（求助人，FR-M4-13）
- **怎么画（结构描述）**：
  1. 系统边界标题"M4 结构化求助★核心闭环"。
  2. `STUDENT` 放左上（同时兼任"求助人"与"回答人"两类语义角色，可在椭圆连线旁加文字区分）；`ALUMNI` 放左下；`ADMIN` 放左中（可发布/回答，权限矩阵允许）；"系统"放右侧。
  3. 边界内按时间轴横向排列（呼应地基§7核心闭环时序）：1 → 2 → 4/5 → 6 → 8/9 → 10 → 11 → 12/13，用例2、3、11 单独标浅色底纹表示"系统自动执行，非用户直接点击"。
  4. 关联线：`STUDENT`/`ALUMNI`/`ADMIN` 都连 1（发布）、4、5（浏览/详情）、6（回答）、7（编辑本人回答）；求助人角色单独连 8（追问）、10（采纳）、12（关闭）、13（撤回）；回答人角色单独连 9（回复追问）。
  5. include 关系：
     - 用例1 `发布求助单` --<<include>>--> 用例2 `求助-校友路由匹配`（发布后同一次调用链内自动触发，§6.1/6.2，必然发生）
     - 用例10 `采纳最佳回答` --<<include>>--> 用例11 `采纳后生成知识候选`（采纳的事务提交后 AAFTER_COMMIT 异步触发，必然发生）
     - 用例8 `提交追问(限次)` 旁批注"<<include>> 限次校验(MAX_FOLLOWUP_PER_ANSWER=3，§6.3)"
  6. extend 关系：
     - 用例3 `路由重试与兜底升级` --<<extend>>--> 用例2（仅当"长时间零应答"条件成立时触发，非每次发布都发生，属扩展场景）
     - 用例12 `关闭求助单` --<<extend>>--> "系统自动关闭"分支（超时无应答/采纳后宽限期到期，§6.5定时任务，区别于求助人手动关闭）
  7. 跨模块批注：用例11 旁画虚线框批注"调用 M3 `KnowledgeEntryService.createFromHelpAdoption`（09设计修订说明R3裁决：M3 owner，自读回答正文）"；"系统"参与者连 2、3、11、12(自动分支) 四处。
  8. 建议在图的角落复刻地基§7核心闭环时序的一句话批注："系统灵魂：发求助→路由通知→回答→采纳→生成候选→审核入库"，标明本图是该闭环的用例级展开。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 3+1 参与者、13 用例椭圆、约 18 条连线（含3条include、2条extend）。
- **工具建议**：drawio；备选 Visio。

---

## 图9 M5 机会与组队 模块用例图

- **图类型**：用例图
- **放报告**：第四章 §3.7
- **要画什么（元素清单）**：
  - **参与者**：`ALUMNI`、`ADMIN`、`STUDENT`；一个**系统**参与者（代表机会状态推进与级联结束定时任务）。
  - **用例椭圆（对应 FR-M5-01~24，共 24 条，其中 FR-M5-21~24 内推相关按 09设计修订说明标注"Could，本期不落代码"）**：
    - **机会**：1 发布机会（ALUMNI/ADMIN已认证）、2 编辑机会（发布人/ADMIN）、3 机会终审（ADMIN，跨M7）、4 手动结束机会（发布人/ADMIN）、5 机会强制下线/维护（ADMIN，跨M7）、6 机会列表(类型筛选/即将截止)（全角色）、7 机会详情查看（全角色）、8 简单报名信令（STUDENT/ALUMNI）、9 定时任务:机会状态推进（系统）、10 定时任务:机会ENDED级联结束队伍（系统）
    - **队伍**：11 发起队伍（STUDENT/ALUMNI已认证）、12 编辑队伍信息（队长）、13 申请加入队伍（STUDENT/ALUMNI已认证）、14 审批加入申请（队长）、15 退出队伍（已批准成员非队长）、16 移除成员（队长）、17 队伍状态流转(停招/开始协作/标记结束)（队长）、18 组队广场列表（登录）、19 队伍详情(含成员列表)（登录）、20 我发起/加入的队伍列表（登录）
    - **内推（Could，本期不落代码，图中虚线椭圆标注）**：21 申请内推、22 内推人更新进度、23 查看我的内推申请单(私密)、24 申请人撤回内推申请
- **怎么画（结构描述）**：
  1. 系统边界标题"M5 机会与组队"。
  2. `ALUMNI` 放左上、`ADMIN` 放右上、`STUDENT` 放左下、"系统"放右下。
  3. 边界内分三区：上区"机会"（用例1–10），中区"队伍"（用例11–20），下区"内推(Could)"（用例21–24，整个分区用虚线大框圈起并标注"本期不落代码，仅设计留存"）。
  4. 关联线：`ALUMNI`/`ADMIN` 连 1、2、4；`ADMIN` 单独连 3、5；全角色（含GUEST，用GUEST连接体现只读）连 6、7；`STUDENT`/`ALUMNI` 连 8、11、13、18、19、20；队长（队伍发起人，`STUDENT`/`ALUMNI`身份下的角色）连 12、14、16、17；已批准成员连 15；`ALUMNI` 连 21（内推人，也可是22的响应方）、`STUDENT`/`ALUMNI` 连 21（申请人）、23、24。
  5. include 关系：
     - 用例11 `发起队伍` --<<include>>--> 延伸小注"校验机会team_required=1且状态∈{ONGOING,CLOSING_SOON}(§6.4)"
     - 用例14 `审批加入申请` --<<include>>--> 延伸小注"current_count CAS防超员(§6.5)"
     - 用例1 `发布机会` --<<include>>--> 延伸小注"referralAvailable=1时校验role=ALUMNI且type=INTERNSHIP(§6.1)"
  6. extend 关系：
     - 用例9 `定时任务:机会状态推进` --<<extend>>--> 用例10（仅当机会转ENDED时才级联触发结束队伍，属条件分支）
     - 用例21 `申请内推` --<<extend>>--> "关联机会(可选)"分支（可独立发起也可挂在某条机会下，§6.8）
     - 用例3 `机会终审` --<<extend>>--> 延伸注"拒绝后可编辑重提"（09设计修订说明S18裁决）
  7. "系统"参与者连 9、10。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 3+1 参与者、24 用例椭圆（其中4个虚线表示Could未落地）、约 26 条连线。
- **工具建议**：drawio（虚线框圈"内推Could分区"表达清晰）；备选 Visio。

---

## 图10 M6 成长时间线 模块用例图

- **图类型**：用例图
- **放报告**：第四章 §3.8
- **要画什么（元素清单）**：
  - **参与者**：`STUDENT`、`ADMIN`；一个**系统**参与者（代表逾期判定与补救优先级计算，本模块无独立定时任务，均随请求实时计算，见M6§9）。
  - **用例椭圆（对应 FR-M6-01~13，共 13 条）**：
    1. 维护时间线模板（ADMIN，FR-M6-01）
    2. 发布/下线时间线模板（ADMIN，FR-M6-02）
    3. 维护时间线节点（ADMIN，FR-M6-03）
    4. 维护节点关联引用（ADMIN，FR-M6-04）
    5. 查看我的成长时间线(动态导航聚合视图)（STUDENT，FR-M6-05）
    6. 预览分化路线内容(决策前对比)（STUDENT，FR-M6-06）
    7. 选择/切换发展路线（STUDENT，FR-M6-07）
    8. 标记/切换节点个人进度（STUDENT，FR-M6-08）
    9. 逾期节点动态判定（系统，随FR-05/10触发，FR-M6-09）
    10. 生成补救优先级提示（系统，FR-M6-10）
    11. 查看整体完成度统计(个人视角)（STUDENT，FR-M6-11）
    12. 查看专业级时间线完成度统计(供运营)（ADMIN，FR-M6-12）
    13. 节点引用计数只读查询(供他模块调用)（系统，FR-M6-13）
- **怎么画（结构描述）**：
  1. 系统边界标题"M6 成长时间线"。
  2. `ADMIN` 放左侧（连模板/节点维护类），`STUDENT` 放右侧（连个人时间线类），"系统"放下方。
  3. 边界内分两区：左区"模板与节点维护"（用例1、2、3、4、12），右区"学生端动态导航"（用例5、6、7、8、9、10、11），用例13放边界底部（跨模块被调用，单独位置）。
  4. 关联线：`ADMIN` 连 1、2、3、4、12；`STUDENT` 连 5、6、7、8、11。
  5. include 关系：
     - 用例5 `查看我的成长时间线` --<<include>>--> 用例9 `逾期节点动态判定`（每次查看都必然计算逾期标记，§6.4）
     - 用例5 --<<include>>--> 用例10 `生成补救优先级提示`（聚合视图组装的一部分，§6.4/6.5）
     - 用例4 `维护节点关联引用` --<<include>>--> 延伸小注"按refType调用M2/M3/M5的existsXxx/getBrief校验ref_id存在性(§6节)"
  6. extend 关系：
     - 用例7 `选择/切换发展路线` --<<extend>>--> "首次访问懒初始化"分支（仅UNDECIDED默认线首次访问才触发，§6.6，非每次都发生）
     - 用例2 `发布/下线时间线模板` --<<extend>>--> "发布前置校验≥1个节点"（§4.1状态机约束，不满足则拒绝）
  7. 跨模块批注：用例13 旁画虚线框批注"供 M2/M3/M5 详情页调用展示'被N个时间线节点引用'（如M3 P09），本模块只做只读聚合"；"系统"参与者连 9、10、13。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 2+1 参与者、13 用例椭圆、约 16 条连线。
- **工具建议**：drawio；备选 Visio。

---

## 图11 M7 平台管理与内容治理 模块用例图

- **图类型**：用例图
- **放报告**：第四章 §3.9
- **要画什么（元素清单）**：
  - **参与者**：`ADMIN`（主）、`STUDENT`/`ALUMNI`（作为举报人/申请人，二者用一个"登录用户(非GUEST)"泛化位表达，或分别画出）；一个**系统**参与者（代表审核任务事件监听与知识候选自动预检、审核吞吐量定时任务）。
  - **用例椭圆（对应 FR-M7-01~21，共 21 条）**：
    1. 统一审核队列（ADMIN，FR-M7-01）
    2. 审核任务详情（ADMIN，FR-M7-02）
    3. 认证申请人工终审（ADMIN，FR-M7-03）
    4. 知识候选终审(隐私checklist驱动)（ADMIN，FR-M7-04）
    5. 知识候选自动完整性/隐私预检(系统触发)（系统，FR-M7-05）
    6. 批量通过（ADMIN，FR-M7-06）
    7. 批量退回（ADMIN，FR-M7-07）
    8. 机会终审（ADMIN，FR-M7-08）
    9. 提交举报（登录用户含STUDENT/ALUMNI/ADMIN不含GUEST，FR-M7-09）
    10. 举报队列/详情(治理端)（ADMIN，FR-M7-10）
    11. 处理举报（ADMIN，FR-M7-11）
    12. 查看我提交的举报记录（举报人，FR-M7-12）
    13. 标签维护(新增/编辑/停用)（ADMIN，FR-M7-13）
    14. 标签管理列表(含使用计数)（ADMIN，FR-M7-14）
    15. 时间线模板维护（ADMIN，FR-M7-15，路由挂本模块/Service复用M6，见09设计修订说明R2）
    16. 时间线节点维护(含关联引用)（ADMIN，FR-M7-16，同上）
    17. 机会内容管理入口（ADMIN，FR-M7-17，复用M5既有CRUD）
    18. 申请贡献者认证（ALUMNI已认证，FR-M7-18）
    19. 贡献者认证审核（ADMIN，FR-M7-19）
    20. 运营数据统计看板（ADMIN，FR-M7-20）
    21. 定时任务:审核吞吐量快照与积压预警（系统，FR-M7-21）
- **怎么画（结构描述）**：
  1. 系统边界标题"M7 平台管理与内容治理（P18管理后台）"。
  2. `ADMIN` 放左侧（覆盖绝大多数用例）；`STUDENT`/`ALUMNI` 放右侧（仅连9、12、18）；"系统"放下方。
  3. 边界内按 P18 五个 Tab 分五区（对应M7§7界面设计"Tab①统一审核队列…"）：
     - Tab①"统一审核队列"区：用例1、2、3、4、5、6、7、8
     - Tab②"举报"区：用例9、10、11、12
     - Tab③"标签体系"区：用例13、14
     - Tab④"时间线模板/节点/机会内容"区：用例15、16、17
     - Tab⑤"贡献者认证与运营统计"区：用例18、19、20、21
  4. 关联线：`ADMIN` 连 1、2、3、4、6、7、8、10、11、13、14、15、16、17、19、20；`STUDENT`/`ALUMNI`/`ADMIN`(作为举报人时) 连 9、12；`ALUMNI` 单独连 18。
  5. 泛化关系（generalization）：用例3 `认证申请人工终审`、用例4 `知识候选终审`、用例8 `机会终审`、用例19 `贡献者认证审核` 均用空心三角箭头指向一个抽象父用例"统一终审决定(decide)"（对应 M7 §6.3 `decide()` 统一入口，四类目标共用一套 `audit_task` CAS+分发机制），体现"一张队列表承接四类终审"的真实架构。
  6. include 关系：
     - 用例1 `统一审核队列` --<<include>>--> 用例2 `审核任务详情`（列表点击进详情，必然路径）
     - 用例3/4/8/19（四类终审）--<<include>>--> "抽象父用例 统一终审决定" 的 include 关系已用泛化表达，这里额外画 用例11 `处理举报` --<<include>>--> 延伸小注"举报处理分发dispatchReportAction，按targetType调用M1/M2/M3/M4/M5对应下架方法(§6.4)"
     - 用例9 `提交举报` --<<include>>--> 延伸小注"同一reporter对同一target的PENDING记录去重合并(§FR-M7-09)"
  7. extend 关系：
     - 用例4 `知识候选终审` --<<extend>>--> "checklist任一勾选强制转退回"分支（三秒可判断隐私checklist，§6.3，覆盖ADMIN原始决定）
     - 用例6/7 `批量通过`/`批量退回` --<<extend>>--> 用例3/4/8（批量操作本质是逐条独立调用同一 `decide()`，仅 KNOWLEDGE_ENTRY/OPPORTUNITY 支持，AUTH_APPLICATION/CONTRIBUTOR_CERT 不支持，见§6.9与30702错误码，需在图上批注此限制）
  8. "系统"参与者连 5、21。
- **可渲染源码或画法**：drawio/Visio 元素清单如上，共 2(+1泛化父用例)+1 参与者、21+1 用例椭圆、约 26 条连线（含1条抽象父用例的4条泛化箭头、3条include、2条extend）。
- **工具建议**：drawio（泛化/include/extend 三种箭头样式在 drawio 用例图模板中都有现成图形，比手工画更规范）；备选 Visio。
